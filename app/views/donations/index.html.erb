<h2 class="mb-3">Donations</h2>

<div class="d-flex flex-column gap-3">
  <%= render partial: "donations/index/donation", collection: @donations, as: :donation %>
</div>

<div class="mt-3 d-flex justify-content-center">
  <%== pagy_bootstrap_nav(@pagy) %>
</div>